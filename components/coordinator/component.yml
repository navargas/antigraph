coordinator:
    build: ./components/coordinator
    environment:
        - USE=repo_adapter/Binary Repo+registry_adapter/Docker Registry
        - LDAPFAIL=snakesnake
    links:
        - authenticator
        - registry_adapter
        - repo_adapter
